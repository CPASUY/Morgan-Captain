package ui;
import java.util.Scanner;
import model.Company;
public class Main {
	//Atributos
		private Company mainCompany;
		
	// variables globales
		public static Scanner lector;
		
	public Main(){
		createCompany();
	}
		public static void main(String[] args) {
			Main objMain=new Main();
		}	
		public void createCompany()
		{
			System.out.println("Enter the name of the company");
			String companyname=lector.nextLine();
			objCompany=new Company (companyname);
		}
		
		public void enterEmpleado(){
			System.out.println("Type the name");
			String name=lector.nextLine();
			System.out.println("Enter the registration number");
			int num=lector.nextLine();
			String numregistration= String.valueOf(num);
			System.out.println("Enter the number of kilos to transport");
			double kilos=lector.nextDouble();
			System.out.println("Enter the total to pay");
			int totalvalue=lector.nextInt();
			System.out.println("Enter the registration day");
			int day=lector.nextInt();
			System.out.println("Enter the registration month");
			int month=lector.nextInt();
			System.out.println("Enter the registration year");
			int year=lector.nextInt();
			
			String mensaje=mainCompany.addEmployee(cedEmpleado,nomEmpleado,apelEmpleado, sexEmpleado, salEmpleado);
			System.out.println(mensaje);
		}
		public boolean embarked(){
			boolean load;
			System.out.println("The client embarks cargo? Type 1 for yes, 2 for no");
			int op=lector.nextInt();
			if(op==1){
				load=true;
			}
			else(op==2){
				load=false;
			}
			return load;
		}
		public void enterLoad(){
			System.out.println("How many boxes does the client ship?");
			int numboxes=lector.nextInt();
			System.out.println("What is the weight of those boxes");
			double weightboxes=lector.nextDouble();
			System.out.println("What is the type of load.Type Dangerous, Perishable or NotPerishable");
			String type=lector.nextLine();
			String mensaje=mainCompany.
		}
